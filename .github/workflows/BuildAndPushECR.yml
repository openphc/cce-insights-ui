name: UI CI and GHCR Push

on:
  push:
    branches: [ "devops/uhis-958" ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Set up Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build
      run: npm run build

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build, Tag & Push Image
      run: |
        IMAGE_URI=${REGISTRY}/${IMAGE_NAME}
        docker build -t ${IMAGE_URI}:latest .
        docker tag ${IMAGE_URI}:latest ${IMAGE_URI}:${IMAGE_TAG}
        docker push ${IMAGE_URI}:${IMAGE_TAG}
        docker push ${IMAGE_URI}:latest

  # trigger-deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/demo-intelligence' && success()
  #   env:
  #     GH_TOKEN: ${{ secrets.CROSS_REPO_TOKEN }}
  #   steps:
  #     - name: Trigger deploy-scripts workflow_dispatch
  #       run: |
  #         gh api --method POST \
  #           /repos/openphc/deploy-scripts/actions/workflows/deploy.yml/dispatches \
  #           --input - <<'JSON'
  #         {
  #           "ref": "main",
  #           "inputs": { "target_env": "dev" }
  #         }
  #         JSON
